/**
 * The above code defines a React functional component named Dashboard that renders a heading element
 * with the text "Welcome to your dashboard!".
 * @returns A React functional component named `Dashboard` is being returned, which renders an `<h1>`
 * element with the text "Welcome to your dashboard!".
 */

import React from 'react';

const Dashboard = () => {
  return <h1>Welcome to your dashboard!</h1>;
};

export default Dashboard;
